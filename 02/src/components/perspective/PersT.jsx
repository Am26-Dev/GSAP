import { assests2 } from "../../assets/assests2";




export const PersT = () => {
  return (
    <div>
        {
            assests2.map((item, index) => (
                <section key={index}>
                    
                </section>
            ))
        }
    </div>
  )
}
